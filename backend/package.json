{
  "name": "growai-backend",
  "version": "1.0.0",
  "description": "API do GrowAI (TrackLink) - monitoramento de estações de cultivo",
  "main": "server.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "db:schema": "psql \"%DATABASE_URL%\" -f db/schema.sql",
    "db:seed": "psql \"%DATABASE_URL%\" -f db/seed.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
